# Kaggle Submission

The benchmark treats each team as a multi-agent policy. Record winner, move count, cooperation events, and the number of generations survived. The starter harness in `benchmarks/kaggle-task.py` is intentionally small so researchers can replace the scorer with a reproducible dataset adapter.
